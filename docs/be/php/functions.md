---
title: Функцыі PHP-пашырэння
description: Даведка па API пашырэння oxphp_sapi для PHP
---

Пашырэнне `oxphp_sapi` для PHP рэгіструе пятнаццаць убудаваных функцый, якія даюць вашаму PHP-коду доступ да ўнутраных механізмаў сервера OxPHP. Гэтыя функцыі даступныя ў кожным PHP-скрыпце, які выконвае OxPHP — дырэктыва `extension=` не патрэбна, бо пашырэнне скампілявана ў карыстальніцкі SAPI.

Плагіны могуць рэгістраваць дадатковыя PHP-функцыі пры запуску. Гэтыя функцыі, прадастаўленыя плагінамі, рэгіструюцца падчас `MINIT` праз C-мост і з'яўляюцца побач з убудаванымі.

## `oxphp_request_id`

Вяртае ўнікальны ідэнтыфікатар запыту, прызначаны бягучаму запыту.

```php
oxphp_request_id(): string
```

**Параметры:** Няма.

**Вяртаемае значэнне:** 20-сімвальны шаснаццатковы радок. Фармат — `{timestamp:08x}{process:04x}{counter:08x}`, дзе першыя 8 сімвалаў — Unix-часовая пазнака, наступныя 4 — ідэнтыфікатар працэсу (PID XOR нанасекунды запуску), а апошнія 8 — манатонны лічыльнік. Вяртае пусты радок, калі ідэнтыфікатар запыту не быў усталяваны (не павінна адбывацца падчас звычайнай апрацоўкі запытаў).

**Прыклад:**

```php
<?php
$requestId = oxphp_request_id();
// "67a3b1c4a1f200000042"

header("X-Request-Id: $requestId");

// Выкарыстанне ў лагіраванні праграмы
error_log("[$requestId] Processing payment for order #1234");
```

**Заўвагі:**
- Ідэнтыфікатар запыту ўсталёўваецца серверам перад пачаткам выканання PHP.
- Той жа ідэнтыфікатар даступны на баку Rust для лагіравання доступу і загалоўкаў адказу.
- Ідэнтыфікатар унікальны паміж працэсамі і перазапускамі дзякуючы кампаненту ідэнтыфікатара працэсу.

---

## `oxphp_worker_id`

Вяртае індэкс патоку PHP-воркера, які апрацоўвае бягучы запыт.

```php
oxphp_worker_id(): int
```

**Параметры:** Няма.

**Вяртаемае значэнне:** Цэлы лік з нулявой базай, які ідэнтыфікуе паток воркера. Значэнні знаходзяцца ў дыяпазоне ад `0` да `PHP_WORKERS - 1` для статычных воркераў. Дынамічныя воркеры атрымліваюць ідэнтыфікатары вышэй за пачатковы дыяпазон.

**Прыклад:**

```php
<?php
$workerId = oxphp_worker_id();
// 3

// Выкарыстанне для каталогаў часовых файлаў, спецыфічных для воркера, або адладкі
$tmpDir = "/tmp/oxphp-worker-$workerId";
```

**Заўвагі:**
- Ідэнтыфікатары воркераў стабільныя на працягу жыцця патоку воркера.
- У рэжыме дынамічнага маштабавання воркеры, створаныя пасля запуску, атрымліваюць нарастаючыя ідэнтыфікатары вышэй за пачатковы памер пула.
- Карысна для адладкі праблем з канкурэнтнасцю або раздзялення рэсурсаў, спецыфічных для воркера.

---

## `oxphp_server_info`

Вяртае асацыятыўны масіў з метаданымі сервера і воркера.

```php
oxphp_server_info(): array
```

**Параметры:** Няма.

**Вяртаемае значэнне:** Асацыятыўны масіў з наступнымі ключамі:

| Ключ | Тып | Апісанне |
|-----|------|-------------|
| `sapi` | `string` | Заўсёды `"oxphp"` |
| `version` | `string` | Версія сервера (цяпер `"0.1.0"`) |
| `worker_id` | `int` | Тое ж значэнне, што і `oxphp_worker_id()` |
| `request_time` | `float` | Unix-часовая пазнака з мікрасекунднай дакладнасцю, калі запыт пачаўся |
| `worker_mode` | `bool` | `true`, калі сервер працуе ў рэжыме воркера, `false` у традыцыйным рэжыме |

**Прыклад:**

```php
<?php
$info = oxphp_server_info();
// [
//     "sapi"         => "oxphp",
//     "version"      => "0.1.0",
//     "worker_id"    => 3,
//     "request_time" => 1738800000.123456,
//     "worker_mode"  => true,
// ]

// Вылічэнне часу, які прайшоў
$elapsed = microtime(true) - $info['request_time'];
echo "Request processing took {$elapsed}s so far";
```

**Заўвагі:**
- `request_time` чытае з лакальнага для патоку сховішча C-бібліятэкі маста, якое ўсталёўваецца перад `php_request_startup()`.
- Гэта значэнне таксама выкарыстоўваецца праверкай `file_update_protection` у OPcache.

---

## `oxphp_request_heartbeat`

Падаўжае тэрмін выканання на зададзеную колькасць секунд ад бягучага моманту. Выкарыстоўвайце ў працяглых скрыптах для прадухілення спынення запыту кааператыўным вартавым таймерам.

```php
oxphp_request_heartbeat(int $time = 10): bool
```

**Параметры:**

| Імя | Тып | Па змаўчанні | Апісанне |
|------|------|---------|-------------|
| `$time` | `int` | `10` | Колькасць секунд для падаўжэння тэрміну ад бягучага моманту |

**Вяртаемае значэнне:** Вяртае `true` пры поспеху. Вяртае `false`, калі `$time` роўны нулю або адмоўны, або калі тэрмін выканання не ўсталяваны.

**Прыклад:**

```php
<?php
// Працяглы імпарт даных
foreach ($records as $record) {
    process($record);
    oxphp_request_heartbeat(30); // Падоўжыць тэрмін на 30 секунд
}
```

**Заўвагі:**
- Тэрмін правяраецца кааператыўна кожныя 128 выклікаў `ub_write` (г.зн. кожныя 128 аперацый вываду). Гэта не жорсткая гарантыя рэальнага часу.
- Пачатковы тэрмін усталёўваецца з `REQUEST_TIMEOUT_SECONDS` да пачатку выканання PHP.
- Калі тайм-аўт не наладжаны (тэрмін роўны 0), выклік гэтай функцыі не мае эфекту і вяртае `false`.

---

## `oxphp_finish_request`

Пазначае бягучы запыт як завершаны, дазваляючы серверу адправіць адказ кліенту, пакуль PHP-скрыпт працягвае выкананне ў фонавым рэжыме. Гэта эквівалент `fastcgi_finish_request()` у OxPHP.

```php
oxphp_finish_request(): bool
```

**Параметры:** Няма.

**Вяртаемае значэнне:** Вяртае `true` пры першым выкліку. Вяртае `false`, калі запыт ужо быў завершаны (г.зн. функцыя была выклікана больш за адзін раз у тым жа запыце).

**Прыклад:**

```php
<?php
// Адправіць адказ кліенту неадкладна
echo json_encode(['status' => 'accepted']);
oxphp_finish_request();

// Працягнуць фонавую працу — кліент ужо атрымаў адказ
send_notification_email($userId);
update_analytics($eventData);
cleanup_temp_files();
```

**Заўвагі:**
- Пасля выкліку гэтай функцыі любы далейшы вывад з `echo` або `print` адкідваецца з адказу кліенту.
- Паток PHP-воркера застаецца занятым да завяршэння выканання скрыпту, таму працяглыя фонавыя задачы памяншаюць даступны пул воркераў.
- Выклік гэтай функцыі двойчы ў тым жа запыце вяртае `false` пры другім выкліку.

---

## `oxphp_is_worker`

Правярае, ці працуе сервер у рэжыме воркера.

```php
oxphp_is_worker(): bool
```

**Параметры:** Няма.

**Вяртаемае значэнне:** `true`, калі бягучы запыт апрацоўваецца персістэнтным воркер-працэсам (г.зн. `WORKER_FILE` усталяваны), `false`, калі сервер працуе ў традыцыйным рэжыме, дзе кожны запыт запускае новы PHP-працэс.

**Прыклад:**

```php
<?php
if (oxphp_is_worker()) {
    // Worker mode: reuse persistent database connection
    $db = $GLOBALS['db'] ?? ($GLOBALS['db'] = new PDO($dsn));
} else {
    // Traditional mode: connect per request
    $db = new PDO($dsn);
}
```

**Заўвагі:**
- Гэтую функцыю можна выклікаць як унутры, так і за межамі callback-апрацоўшчыка `oxphp_worker()`.
- Тое ж значэнне даступна праз `oxphp_server_info()['worker_mode']`.
- Карысна для бібліятэк і фрэймворкаў, якім трэба адаптаваць паводзіны ў залежнасці ад мадэлі выканання (напр., аб'яднанне злучэнняў, статычныя кэшы, апрацоўка сесій).

---

## `oxphp_is_streaming`

Правярае, ці знаходзіцца бягучы запыт у рэжыме стрымінгу.

```php
oxphp_is_streaming(): bool
```

**Параметры:** Няма.

**Вяртаемае значэнне:** `true`, калі рэжым стрымінгу актыўны, `false` у адваротным выпадку.

**Прыклад:**

```php
<?php
if (oxphp_is_streaming()) {
    // Інкрыментальны скід вываду
    echo "data: " . json_encode($event) . "\n\n";
    flush();
} else {
    // Буферызаваць поўны адказ
    echo json_encode($allData);
}
```

**Заўвагі:**
- Рэжым стрымінгу актывуецца аўтаматычна пры ўстаноўцы загалоўка `Content-Type: text/event-stream` або ўручную праз `oxphp_stream_flush()`.
- Гэта функцыя карысная для скрыптоў, якім трэба адаптаваць паводзіны вываду ў залежнасці ад рэжыму транспарту.

---

## `oxphp_stream_flush`

Актывуе рэжым стрымінгу (калі ён яшчэ не актыўны) і скідвае бягучы буфер вываду кліенту ў выглядзе чанка. Гэта асноўная функцыя для рэалізацыі Server-Sent Events (SSE) у OxPHP.

```php
oxphp_stream_flush(): bool
```

**Параметры:** Няма.

**Вяртаемае значэнне:** Вяртае `true` пры поспеху. Вяртае `false`, калі запыт ужо быў завершаны праз `oxphp_finish_request()`.

**Прыклад:**

```php
<?php
header('Content-Type: text/event-stream');
header('Cache-Control: no-cache');

for ($i = 0; $i < 10; $i++) {
    echo "id: $i\n";
    echo "data: " . json_encode(['counter' => $i, 'time' => microtime(true)]) . "\n\n";
    oxphp_stream_flush();
    sleep(1);
}
```

**Як гэта працуе:**

1. Пры першым выкліку актывуе рэжым стрымінгу праз C-мост (`oxphp_bridge_set_stream_mode`)
2. Скідвае ўсе буферы вываду PHP (`php_output_flush_all`)
3. Выклікае callback SAPI flush, які адпраўляе буферызаваны вывад як HTTP-чанк кліенту

**Заўвагі:**
- Загалоўкі адпраўляюцца кліенту пры першым скідзе. Наступныя выклікі адпраўляюць толькі чанкі цела.
- Можна таксама выкарыстоўваць натыўны PHP `flush()` з `Content-Type: text/event-stream` — OxPHP аўтаматычна вызначае SSE content type і актывуе рэжым стрымінгу. У гэтым выпадку спачатку выклічце `ob_end_flush()`, каб адключыць буферызацыю вываду PHP.
- Калі `oxphp_finish_request()` быў выкліканы раней, функцыя вяртае `false` і нічога не робіць.
- HTTP-злучэнне закрываецца аўтаматычна, калі PHP-скрыпт завяршаецца і канал стрымінгу закрываецца.
- Зваротны ціск забяспечваецца праз абмежаваны канал (ёмістасць 64) — калі кліент павольны, `oxphp_stream_flush()` блакуецца, пакуль кліент не дагоніць.

---

## `oxphp_sleep`

Кааператыўны сон, які прыпыняе бягучы файбер, дазваляючы патоку воркера апрацоўваць іншыя запыты падчас чакання. Адкатваецца да блакіруючага `usleep()`, калі выклікаецца па-за файберам.

```php
oxphp_sleep(float $seconds): void
```

**Параметры:**

| Імя | Тып | Апісанне |
|------|------|-------------|
| `$seconds` | `float` | Працягласць сну ў секундах (напрыклад, `0.5` для 500мс) |

**Вяртаемае значэнне:** Нічога (void).

**Прыклад:**

```php
<?php
oxphp_worker(function() {
    header('Content-Type: text/event-stream');
    for ($i = 0; $i < 10; $i++) {
        echo "data: " . json_encode(['counter' => $i]) . "\n\n";
        oxphp_stream_flush();
        oxphp_sleep(1.0); // yields fiber, worker handles other requests
    }
});
```

**Заўвагі:**
- У рэжыме воркера з актыўным планіроўшчыкам файбераў функцыя рэгіструе таймер і прыпыняе бягучы файбер. Планіроўшчык узнаўляе файбер пасля ўказанай працягласці.
- Па-за файберам (традыцыйны рэжым) адкатваецца да блакіруючага `usleep()`.
- Значэнні, роўныя нулю або адмоўныя, вяртаюцца неадкладна без эфекту.
- Дакладнасць таймера — мілісекундная (працягласці акругляюцца ўверх да бліжэйшай мілісекунды).

---

## `oxphp_usleep`

Кааператыўны мікрасекундны сон. Ідэнтычны `oxphp_sleep()`, але прымае мікрасекунды як цэлы лік.

```php
oxphp_usleep(int $microseconds): void
```

**Параметры:**

| Імя | Тып | Апісанне |
|------|------|-------------|
| `$microseconds` | `int` | Працягласць сну ў мікрасекундах |

**Вяртаемае значэнне:** Нічога (void).

**Прыклад:**

```php
<?php
oxphp_worker(function() {
    oxphp_usleep(50000); // 50ms cooperative sleep
    echo "done";
});
```

**Заўвагі:**
- Паводзіны ідэнтычныя `oxphp_sleep()` з мікрасекунднай дакладнасцю.
- У рэжыме файбераў таймер рэгіструецца з мілісекунднай дакладнасцю (акругляецца ўверх).
- Значэнні, роўныя нулю або адмоўныя, вяртаюцца неадкладна без эфекту.

---

## `oxphp_worker`

Уваходзіць у цыкл рэжыму персістэнтнага воркера. Выклікае наданы callback-апрацоўшчык для кожнага ўваходнага HTTP-запыту. Паміж запытамі мяккі скід ачышчае стан кожнага запыту (суперглабалы, буферы вываду, загалоўкі адказу, стан памылак) без знішчэння PHP-кучы, таму стан загрузкі (аўтазагрузчыкі, злучэнні з базай даных, кэшаваная канфігурацыя) захоўваецца паміж запытамі.

```php
oxphp_worker(callable $handler): bool
```

**Параметры:**

| Імя | Тып | Апісанне |
|------|------|-------------|
| `$handler` | `callable` | Callback, які выклікаецца адзін раз на кожны HTTP-запыт. Не атрымлівае аргументаў. |

**Вяртаемае значэнне:** Вяртае `true` пры плаўнай спынцы (спыненне сервера). Вяртае `false` неадкладна, калі рэжым воркера не ўключаны (г.зн. `WORKER_FILE` не ўсталяваны).

**Прыклад:**

```php
<?php
// worker.php — кропка ўваходу персістэнтнага воркера

// Загрузка: выконваецца адзін раз за жыццё воркера
require __DIR__ . '/vendor/autoload.php';
$db = new PDO('mysql:host=localhost;dbname=app', 'root', '');
$config = json_decode(file_get_contents(__DIR__ . '/config.json'), true);

// Апрацоўка запытаў у цыкле
oxphp_worker(function () use ($db, $config) {
    $uri = $_SERVER['REQUEST_URI'];
    $method = $_SERVER['REQUEST_METHOD'];

    // Маршрутызацыя і апрацоўка запыту
    if ($uri === '/api/users' && $method === 'GET') {
        $users = $db->query('SELECT id, name FROM users')->fetchAll();
        header('Content-Type: application/json');
        echo json_encode($users);
    } else {
        http_response_code(404);
        echo 'Not Found';
    }
});
```

**Як гэта працуе:**

1. Callback-апрацоўшчык выклікаецца для кожнага HTTP-запыту, атрыманага ад узроўню Rust.
2. Паміж запытамі адбываецца мяккі скід:
   - Суперглабалы (`$_GET`, `$_POST`, `$_SERVER`, `$_COOKIE`, `$_FILES`) запаўняюцца данымі новага запыту
   - Буферы вываду ачышчаюцца
   - Загалоўкі HTTP-адказу скідваюцца
   - Функцыі, зарэгістраваныя праз `register_shutdown_function()`, выклікаюцца і ачышчаюцца
3. Зборка смецця запускаецца перыядычна (кожныя 100 запытаў) для вызвалення цыклічных спасылак без уплыву на затрымку кожнага запыту.
4. Цыкл завяршаецца, калі:
   - Сервер спыняецца (сігнал плаўнай спынкі)
   - Апрацоўшчык завяршаецца памылкай 3 разы запар (прычына выхаду: `consecutive_errors`) — ізаляваныя памылкі дапускаюцца, лічыльнік скідваецца пры поспеху
   - Воркер дасягае `WORKER_MAX_REQUESTS` (прычына выхаду: `max_requests`)
   - Воркер перавышае `WORKER_MAX_MEMORY_MIB` (прычына выхаду: `max_memory`)

**Заўвагі:**
- Гэта функцыя працуе толькі калі ўсталяваны `WORKER_FILE`. Яе выклік з звычайнага PHP-скрыпту выдае `E_WARNING` і вяртае `false`.
- Зменныя, аб'яўленыя за межамі замыкання апрацоўшчыка, захоўваюцца паміж запытамі. Выкарыстоўвайце гэта для злучэнняў з базай даных, канфігурацыі і іншай затратнай ініцыялізацыі.
- Клаўзула `use` апрацоўшчыка захоплівае зменныя па спасылцы або значэнні як звычайна. Зменныя, захопленыя па спасылцы, разалоўваюць стан паміж запытамі.
- Рэцыклінг воркера (праз `WORKER_MAX_REQUESTS` або `WORKER_MAX_MEMORY_MIB`) прыводзіць да выхаду і перазапуску працэсу воркера, які паўторна выконвае ўвесь скрыпт воркера, уключаючы загрузку.
- Метрыкі рэжыму воркера (`oxphp_worker_requests_handled_total`, `oxphp_worker_recycles_total` і інш.) даступныя на канцавой кропцы `/metrics`, калі ўнутраны сервер працуе.

---

## `oxphp_async`

Адпраўляе замыканне для асінхроннага выканання на выдзеленым пуле асінхронных воркераў. Зменныя `use` замыкання і аргументы серыялізуюцца на зыходным патоку і дэсерыялізуюцца як незалежныя копіі на патоку асінхроннага воркера.

```php
oxphp_async(Closure $closure, mixed ...$args): int|false
```

**Параметры:**

| Імя | Тып | Апісанне |
|------|------|-------------|
| `$closure` | `Closure` | Замыканне для выканання на патоку асінхроннага воркера |
| `...$args` | `mixed` | Аргументы, серыялізаваныя ў воркер. Падтрымліваюцца скаляры, радкі і масівы. Рэсурсы і аб'екты адхіляюцца з `E_WARNING` |

**Вяртаемае значэнне:** Ідэнтыфікатар проміса (дадатнае цэлае) пры поспеху. Вяртае `false`, калі асінхронны пул не сканфігураваны (`ASYNC_WORKERS=0`) або чарга поўная.

**Прыклад:**

```php
<?php
$promise = oxphp_async(function(int $x, int $y): int {
    return $x + $y;
}, 10, 20);

$result = oxphp_async_await($promise);
// 30
```

**Заўвагі:**
- Замыканне выконваецца на асобным патоку АС з уласным станам PHP ZTS. Зменныя, захопленыя праз `use`, серыялізуюцца як незалежныя копіі — зыходныя зменныя застаюцца запісваемымі.
- Асінхронны пул павінен быць уключаны праз зменную асяроддзя `ASYNC_WORKERS`.
- Калі чарга поўная, задача адхіляецца і `oxphp_async()` вяртае `false`.

---

## `oxphp_async_await`

Блакуе бягучы паток да завяршэння ўказанай асінхроннай задачы і вяртае яе вынік.

```php
oxphp_async_await(int $promise_id, ?float $timeout = null): mixed
```

**Параметры:**

| Імя | Тып | Па змаўчанні | Апісанне |
|------|------|---------|-------------|
| `$promise_id` | `int` | *(абавязкова)* | Ідэнтыфікатар проміса, вернуты `oxphp_async()` |
| `$timeout` | `?float` | `null` | Максімум секунд для чакання. `null` чакае бясконца |

**Вяртаемае значэнне:** Вяртаемае значэнне замыкання. Падтрымліваюцца ўсе скалярныя тыпы, радкі і масівы (уключаючы ўкладзеныя).

**Выключэнні:**

| Выключэнне | Умова |
|-----------|-----------|
| `OxPHP\AsyncException` | Замыканне выклікала выключэнне або выклікала `die()` / `exit()` |
| `OxPHP\AsyncTimeoutException` | Тайм-аўт скончыўся да завяршэння задачы |

**Прыклад:**

```php
<?php
$p = oxphp_async(function(): string {
    usleep(100_000);
    return 'done';
});

try {
    $result = oxphp_async_await($p, 0.5); // 500ms тайм-аўт
} catch (\OxPHP\AsyncTimeoutException $e) {
    $result = 'timed out';
}
```

**Заўвагі:**
- Вяртаемае значэнне дэсерыялізуецца з патоку асінхроннага воркера на кучу бягучага патоку.
- Кожны ідэнтыфікатар проміса можа быць чаканы толькі адзін раз. Чаканне таго ж ідэнтыфікатара двойчы прыводзіць да нявызначаных паводзін.
- Нечаканыя промісы аўтаматычна ачышчаюцца ў канцы запыту (RSHUTDOWN) з 5-секундным тайм-аўтам.

---

## `oxphp_async_await_all`

Чакае некалькі промісаў і вяртае ўсе вынікі ў выглядзе асацыятыўнага масіву.

```php
oxphp_async_await_all(array $promise_ids, ?float $timeout = null): array
```

**Параметры:**

| Імя | Тып | Па змаўчанні | Апісанне |
|------|------|---------|-------------|
| `$promise_ids` | `array` | *(абавязкова)* | Масіў ідэнтыфікатараў промісаў, вернутых `oxphp_async()` |
| `$timeout` | `?float` | `null` | Тайм-аўт у секундах для кожнага проміса |

**Вяртаемае значэнне:** Асацыятыўны масіў, які супастаўляе кожны ідэнтыфікатар проміса з яго вынікам.

**Выключэнні:** `OxPHP\AsyncException` або `OxPHP\AsyncTimeoutException`, калі любы проміс завяршаецца з памылкай або перавышае тайм-аўт.

**Прыклад:**

```php
<?php
$p1 = oxphp_async(function(): int { return 1; });
$p2 = oxphp_async(function(): int { return 2; });
$p3 = oxphp_async(function(): int { return 3; });

$results = oxphp_async_await_all([$p1, $p2, $p3]);
// [$p1 => 1, $p2 => 2, $p3 => 3]
```

---

## `oxphp_async_await_any`

Спаборніцтва некалькіх промісаў — вяртае першы завершаны, незалежна ад парадку ў масіве. Унутрана выкарыстоўвае `futures::select_all` для сапраўднай семантыкі канкурэнтнага спаборніцтва.

```php
oxphp_async_await_any(array $promise_ids, ?float $timeout = null): array
```

**Параметры:**

| Імя | Тып | Па змаўчанні | Апісанне |
|------|------|---------|-------------|
| `$promise_ids` | `array` | *(абавязкова)* | Масіў ідэнтыфікатараў промісаў, вернутых `oxphp_async()` |
| `$timeout` | `?float` | `null` | Агульны тайм-аўт у секундах для завяршэння любога проміса |

**Вяртаемае значэнне:** Асацыятыўны масіў з двума ключамі: `id` (ідэнтыфікатар проміса, які завяршыўся першым) і `value` (яго вынік).

**Выключэнні:** `OxPHP\AsyncException`, калі проміс-пераможца выклікаў выключэнне. `OxPHP\AsyncTimeoutException`, калі ніводны проміс не завяршыўся ў межах тайм-аўту.

**Астатнія промісы:** Промісы, якія не перамаглі, застаюцца актыўнымі і могуць быць чаканыя паасобку праз `oxphp_async_await()`. Пры тайм-аўце ўсе ўказаныя промісы скасоўваюцца.

**Прыклад:**

```php
<?php
$p1 = oxphp_async(function(): int { sleep(2); return 1; });
$p2 = oxphp_async(function(): int { usleep(100_000); return 2; });
$p3 = oxphp_async(function(): int { sleep(1); return 3; });

$winner = oxphp_async_await_any([$p1, $p2, $p3]);
// ['id' => $p2, 'value' => 2]  (найхутчэйшы, ~100ms)

// Промісы, якія не перамаглі, усё яшчэ даступныя для чакання
$r1 = oxphp_async_await($p1); // 1
$r3 = oxphp_async_await($p3); // 3
```

---

## Функцыі плагінаў

Плагіны могуць рэгістраваць карыстальніцкія PHP-функцыі, якія можна выклікаць з вашых скрыптоў. Гэтыя функцыі рэгіструюцца падчас ініцыялізацыі PHP-модуля (`MINIT`) і адпраўляюцца праз C-мост у код апрацоўшчыка на Rust.

Функцыі плагінаў выкарыстоўваюць натыўны мост для дыспетчарызацыі з нулявой серыялізацыяй. Аргументы і вяртаемыя значэнні перадаюцца як сырыя ўказальнікі `zval` — Rust чытае і піша іх напрамую праз C-функцыі доступу, без накладных выдаткаў на JSON-кадаванне. Калі апрацоўшчык вяртае памылку, PHP выдае `E_WARNING`, і вяртаецца `NULL`.

```php
<?php
// Прыклад: выклік функцыі, зарэгістраванай плагінам
$result = some_plugin_function('arg1', 42, ['key' => 'value']);
```

Функцыі плагінаў пералічаны побач з убудаванымі функцыямі ў вывадзе `phpinfo()`, але яны зарэгістраваны глабальна (не пад пашырэннем `oxphp_sapi`), таму яны не з'яўляюцца ў `get_extension_funcs('oxphp_sapi')`.

## Інфармацыя аб пашырэнні

Метаданыя пашырэння бачныя ў вывадзе `phpinfo()`:

| Поле | Значэнне |
|-------|-------|
| Назва пашырэння | `oxphp_sapi` |
| Версія | `0.1.0` |

Вы можаце праверыць, што пашырэнне загружана:

```php
<?php
var_dump(extension_loaded('oxphp_sapi'));
// bool(true)

// Спіс убудаваных функцый пашырэння
print_r(get_extension_funcs('oxphp_sapi'));
// Array
// (
//     [0] => oxphp_request_id
//     [1] => oxphp_worker_id
//     [2] => oxphp_server_info
//     [3] => oxphp_request_heartbeat
//     [4] => oxphp_finish_request
//     [5] => oxphp_is_worker
//     [6] => oxphp_is_streaming
//     [7] => oxphp_stream_flush
//     [8] => oxphp_sleep
//     [9] => oxphp_usleep
//     [10] => oxphp_worker
//     [11] => oxphp_async
//     [12] => oxphp_async_await
//     [13] => oxphp_async_await_all
//     [14] => oxphp_async_await_any
// )
```

## Глядзіце таксама

- [Асінхронныя промісы](/be/features/async-promises.md) --- паралельнае выкананне з `oxphp_async()` і `oxphp_async_await()`
- [Мультыплексаванне запытаў на файберах](/features/fiber-multiplexing.md) --- кааператыўная шматзадачнасць з `oxphp_sleep()` і файбер-сумяшчальны `oxphp_async_await()`
- [Суперглабальныя зменныя](superglobals.md) --- як OxPHP запаўняе `$_SERVER`, `$_GET`, `$_POST` і іншыя суперглабальныя зменныя
- [Сумяшчальнасць з OPcache](opcache.md) --- як зваротны выклік `request_time` забяспечвае працу OPcache
- [Ідэнтыфікатары запытаў](/be/features/request-ids.md) --- як генеруюцца і перадаюцца ідэнтыфікатары запытаў
- [SAPI-мост](/be/architecture/sapi-bridge.md) --- C-мост, які злучае Rust і PHP
- [Пул воркераў](/be/architecture/worker-pool.md#рэжым-воркера-persistent-php) --- архітэктура рэжыму воркера, рэцыклінг і метрыкі
- [Канфігурацыя](/be/operations/configuration.md#рэжым-воркера) --- `WORKER_FILE`, `WORKER_MAX_REQUESTS`, `WORKER_MAX_MEMORY_MIB`
