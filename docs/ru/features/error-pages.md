---
title: Пользовательские страницы ошибок
description: Обслуживание брендированных HTML-страниц ошибок для ответов 4xx и 5xx в OxPHP — загружаются один раз при запуске и обслуживаются из памяти.
---

# Пользовательские страницы ошибок

OxPHP обслуживает брендированные HTML-страницы ошибок для ответов 4xx и 5xx. Страницы ошибок загружаются с диска один раз при запуске и обслуживаются из памяти, поэтому при обработке запросов операции ввода-вывода не выполняются.

## Как это работает

1. При запуске OxPHP читает директорию, указанную в `ERROR_PAGES_DIR`, и загружает в память все допустимые файлы `{status}.html`.
2. Файлы должны быть названы числовым кодом HTTP-статуса в диапазоне 400–599 (например, `404.html`, `503.html`). Файлы с нечисловыми именами, кодами статуса вне этого диапазона (включая `200.html`) или с расширениями, отличными от `.html`, молча игнорируются.
3. Когда OxPHP генерирует ответ 4xx или 5xx, он проверяет наличие предзагруженной страницы ошибки. При её наличии OxPHP заменяет только тело и описывающие его заголовки: `Content-Type` устанавливается в `text/html; charset=utf-8`, `Content-Length` — в размер страницы, а заголовки, связанные с исходным телом (`Content-Encoding`, `ETag`, `Last-Modified`), удаляются, чтобы не помечать и не ревалидировать замену неверно (например, тело ошибки PHP, сжатое `ob_gzhandler`, не оставит HTML-страницу с заголовком `Content-Encoding: gzip`). Заголовки, описывающие семантику ответа, а не тело, сохраняются в пользовательской странице — `Content-Range` для `416 Range Not Satisfiable`, `Retry-After` для `529 Site is overloaded` и `Allow` для `405 Method Not Allowed`.
4. Если директория не существует или не может быть прочитана при запуске, OxPHP записывает предупреждение в лог и продолжает работу без пользовательских страниц ошибок. Ответы об ошибках при этом используют текстовые тела, пока директория не будет исправлена и сервер не будет перезапущен.

## Конфигурация

| Переменная | По умолчанию | Описание |
|------------|-------------|----------|
| `ERROR_PAGES_DIR` | *(не задана)* | Директория, содержащая HTML-файлы пользовательских страниц ошибок. Файлы должны называться `{status}.html` для кодов статуса 400–599. Если не задана, ответы об ошибках используют текстовые тела |

## Примеры страниц

Каждая страница ошибки — самодостаточный HTML-файл с именем `{status}.html`. Держите их со встроенными стилями и без внешних ресурсов: вторичный запрос, который сам завершится ошибкой, иначе сломает саму страницу ошибки.

### Переиспользуемый шаблон

Скопируйте это в каждый `{status}.html` и меняйте `<title>`, `<h1>` и `<p>`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>500 — Внутренняя ошибка сервера</title>
  <style>
    body { font-family: system-ui, sans-serif; text-align: center; padding: 4rem 1rem; color: #333; }
    h1 { font-size: 2rem; margin-bottom: 0.5rem; }
    p { color: #666; }
  </style>
</head>
<body>
  <h1>Что-то пошло не так</h1>
  <p>Попробуйте ещё раз через минуту.</p>
</body>
</html>
```

### Какие коды статуса имеет смысл добавить

OxPHP заменяет тело любого ответа 4xx или 5xx, который доходит до конвейера ответов. Эти коды сервер отдаёт сам, поэтому добавьте файл под каждый:

| Файл | Статус | Когда OxPHP его возвращает |
|------|--------|----------------------------|
| `404.html` | Not Found | Нет подходящего файла или маршрута; заблокированный dotfile (`.env`, `.git/`); прямой запрос `.php` в режиме Framework; ответ по умолчанию для `PHP_DENY_PATHS` |
| `413.html` | Payload Too Large | Тело запроса превышает максимальный размер |
| `416.html` | Range Not Satisfiable | Невыполнимый заголовок `Range` для статического файла (`Content-Range` сохраняется) |
| `500.html` | Internal Server Error | Необработанная или фатальная ошибка PHP |
| `503.html` | Service Unavailable | Плавное завершение (drain) при остановке |
| `504.html` | Gateway Timeout | Запрос превысил `REQUEST_TIMEOUT_SECONDS` |
| `529.html` | Site is overloaded | Очередь запросов заполнена при `QUEUE_CAPACITY` (`Retry-After` сохраняется) |

Любой другой код 4xx или 5xx работает так же — добавьте `403.html`, `451.html` и т. д. для кодов, которые возвращает ваше PHP-приложение, или для кастомного статуса `PHP_DENY_FALLBACK`. Единственное исключение — `429` от ограничителя частоты: он формируется до запуска этого обработчика и всегда использует стандартное тело (см. раздел «Примечания» ниже).

### Готовые примеры

Минимальная страница 404:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>404 — Страница не найдена</title>
  <style>
    body { font-family: system-ui, sans-serif; text-align: center; padding: 4rem 1rem; color: #333; }
    h1 { font-size: 2rem; margin-bottom: 0.5rem; }
    p { color: #666; }
  </style>
</head>
<body>
  <h1>Страница не найдена</h1>
  <p>Запрошенная страница не существует.</p>
</body>
</html>
```

Страница обслуживания 503 с автообновлением:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta http-equiv="refresh" content="30">
  <title>503 — Сервис недоступен</title>
  <style>
    body { font-family: system-ui, sans-serif; text-align: center; padding: 4rem 1rem; color: #333; }
    h1 { font-size: 2rem; margin-bottom: 0.5rem; }
    p { color: #666; }
  </style>
</head>
<body>
  <h1>Сервис недоступен</h1>
  <p>Идут технические работы. Страница обновится автоматически.</p>
</body>
</html>
```

## Устранение неполадок

### Пользовательские страницы ошибок не отображаются

Убедитесь, что переменная `ERROR_PAGES_DIR` задана, а файлы названы правильно.

**Проверьте:** Подтвердите активный путь к директории и что OxPHP вывел строки "Loaded custom error page" при запуске:

```bash
docker logs my-app 2>&1 | grep "error page"
```

**Исправление:** Убедитесь, что путь к директории верен, файлы названы `{status}.html`, а контейнер имеет доступ на чтение директории.

### Предупреждение при запуске об отсутствующей директории страниц ошибок

OxPHP записывает предупреждение в лог и продолжает работу без пользовательских страниц ошибок, если директория `ERROR_PAGES_DIR` не существует или не может быть прочитана. В этом случае ответы об ошибках используют текстовые тела. Проверьте правильность монтирования тома в Docker:

```bash
docker run --rm -v ./errors:/var/www/errors:ro \
  -e ERROR_PAGES_DIR=/var/www/errors \
  ghcr.io/oxphp/oxphp:0.9.0
```

### Ответ 429 всё равно показывает стандартное тело

Некоторые ответы, генерируемые до запуска конвейера ответов — например, отказы от ограничителя частоты — не обрабатываются обработчиком страниц ошибок. Ответ `429 Too Many Requests` от ограничителя частоты использует стандартное тело независимо от наличия файла `429.html`.

## Пример Docker

```yaml
services:
  app:
    image: ghcr.io/oxphp/oxphp:0.9.0
    ports:
      - "8080:8080"
    volumes:
      - ./src:/var/www/html:ro
      - ./errors:/var/www/errors:ro
    environment:
      ERROR_PAGES_DIR: "/var/www/errors"
      ENTRY_FILE: "index.php"
```

Структура директорий:

```text
project/
  src/
    public/
      index.php
  errors/
    403.html
    404.html
    500.html
    503.html
    504.html
    529.html
```

## Лучшие практики

- Делайте страницы ошибок самодостаточными со встроенным CSS. Не ссылайтесь на внешние таблицы стилей или скрипты — эти вторичные запросы сами могут завершиться неудачей.
- Добавляйте тег `<meta http-equiv="refresh" content="30">` на страницу `503.html`, чтобы пользователи автоматически повторяли попытку после завершения обслуживания.
- Держите страницы ошибок небольшими. Каждая загруженная страница хранится в памяти на всё время работы серверного процесса.

## Примечания

Пользовательские страницы ошибок применяются к ответам, проходящим через обычный конвейер обработки запросов. Ответ `429 Too Many Requests` от ограничителя частоты генерируется до запуска обработчика страниц ошибок и использует стандартное текстовое тело.

## См. также

- [Маршрутизация](routing.md) -- как генерируются ответы 404 для несовпадающих путей
- [Ограничение частоты запросов](rate-limiting.md) -- поведение ограничителя и ответы 429
- [Справочник по конфигурации](../operations/configuration.md) -- полный справочник переменных окружения
