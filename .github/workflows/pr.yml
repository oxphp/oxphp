name: PR Checks

on:
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo fmt -- --check

  clippy:
    name: Clippy (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: no-default
            flags: "--no-default-features --all-targets"
          - name: default
            flags: "--all-targets"
    steps:
      - uses: actions/checkout@v4
      - name: Rust version
        id: rust
        run: echo "hash=$(rustc --version | sha256sum | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: clippy-${{ matrix.name }}-${{ steps.rust.outputs.hash }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: clippy-${{ matrix.name }}-${{ steps.rust.outputs.hash }}-
      - name: Clippy
        env:
          CLIPPY_FLAGS: ${{ matrix.flags }}
        run: cargo clippy $CLIPPY_FLAGS -- -D warnings

  # Tests run with --no-default-features because default features link against
  # libphp.so, which isn't installed on the CI runner. The clippy "default" job
  # above type-checks the plugin-apm / plugin-async paths that this omits.
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Rust version
        id: rust
        run: echo "hash=$(rustc --version | sha256sum | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: test-${{ steps.rust.outputs.hash }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: test-${{ steps.rust.outputs.hash }}-
      - run: cargo test --no-default-features
