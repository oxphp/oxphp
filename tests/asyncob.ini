; Force unlimited PHP output buffering so a background task's echo accumulates
; in the C output-buffer layer (the observed RSS leak path). The default
; (output_buffering=0) would route echo straight to the Rust RESPONSE buffer.
output_buffering = On
