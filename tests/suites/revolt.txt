# profile: revolt
# description: revolt/event-loop under worker mode — per-request isolation
#
# revolt/event-loop v1.0.9 is vendored at tests/fixtures/revolt (MIT).
#
# Characterization first: it establishes that Revolt runs at all in a worker
# request and why (\Fiber::getCurrent() is null there, so Revolt sees {main}).
# The two that follow are RED — they assert the isolation that follows from
# "one request, one context", which the shared {main} key does not provide.

revolt/test_worker_request_looks_like_main
revolt/test_suspension_not_shared_across_requests
revolt/test_fiberlocal_not_leaked_across_requests
