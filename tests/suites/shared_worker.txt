# profile: worker
# description: Shared state primitives — Channel fiber tests
#
# Both tests dispatch fiber producers/consumers via oxphp_async() with the
# Shared\Channel captured by `use ($ch)`. The closure's static-vars round trip
# through the async pool currently restores $ch as NULL — the Shareable handle
# is dropped between serialize and deserialize on the worker thread, so the
# fiber blows up on `Call to a member function send()/close() on null`.
# Disable until the static-vars Shareable rebuild is fixed in
# src/executor/async_pool.rs / src/plugins/ox_async/functions.rs.
#
# shared/test_channel_fiber_producer_consumer
# shared/test_channel_shutdown_drain
