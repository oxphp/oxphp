# profile: async1
# description: Fan-out await composition on a single async worker

async/test_await_all_compose
async/test_await_all_timeout_in_fiber
async/test_await_race_compose
async/test_await_any_compose
async/test_cancel_cpu_bound

# Driver idle interval: it widens while nothing happens, but never past a
# deadline this worker set itself, and a task dispatched into that wait is taken
# off the queue by the wait rather than lost with it.
async/test_task_sleep_precision
async/test_task_dispatched_into_idle_wait

# Timer poll delivers at most 32 ids per call; more sleepers than that expiring
# in one window must all still wake — the tail stays registered for the next poll.
async/test_timer_poll_beyond_32
