; The trap in php/bailout/test_headers_bailout.php calibrates itself against
; whatever the allocator has mapped, so it does not depend on this number. The
; cap is here to bound the ballast loop if the calibration ever misses: without
; one it would fill until the engine's default ceiling instead of failing fast.
memory_limit = 32M
