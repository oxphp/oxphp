<?php

declare(strict_types=1);

// No TestCase — the fatal error IS the test.
// The runner expects HTTP 500. The script must not produce valid JSON output.
undefined_function_that_does_not_exist();
